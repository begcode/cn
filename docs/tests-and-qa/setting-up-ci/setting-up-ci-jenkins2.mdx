---
title: 在 Jenkins 2 上设置持续集成
slug: /setting-up-ci-jenkins2/
last_update:
  date: 2017-01-19T14:15:00-00:00
---

## 安装 Jenkins 2

### 标准安装

在你的机器上安装 JDK 8。

去官方网站 [https://jenkins.io/2.0/](https://jenkins.io/2.0/)

下载 `jenkins.war`

### With Docker

Launch the [Docker image](https://hub.docker.com/r/jenkins/jenkins) 
(_the default port has been changed to 18080 since the JHipster app is configured to run on 8080_)

```shell
docker container run -d --name jenkins2 -p 18080:8080 -p 50000:50000 jenkins/jenkins
```

然后你可以在以下地址访问 Jenkins 仪表板：
- http://localhost:18080 (on MacOS & Linux)
- http://192.168.99.100:18080 (on Windows)
  - 如果这不起作用，请将 `192.168.99.100` 替换为你的 docker 默认 ip 地址：`docker-machine ip default`

注意：你将被要求输入 `initialAdminPassword`，你可以在容器启动时的日志中找到它。
你也可以通过 `docker logs jenkins2` 访问它
例如：

```
*************************************************************
*************************************************************
*************************************************************

Jenkins initial setup is required. An admin user has been created and a password generated.
Please use the following password to proceed to installation:

6707db8735be4ee29xy056f65af6ea13

This may also be found at: /var/jenkins_home/secrets/initialAdminPassword

*************************************************************
*************************************************************
*************************************************************
```


创建新作业

- 添加新项目
  - 输入项目名称
  - 选择流水线
  - 点击确定

![Jenkins2 item](/images/jenkins2_add_item.png)

- 定义：来自 SCM 的流水线脚本
- SCM：Git
- 仓库
  - 仓库 URL：在此选择你的仓库

![Jenkins2 pipeline](/images/jenkins2_pipeline.png)


## Jenkinsfile

![Jenkins2 result](/images/jenkins2_result.png)
