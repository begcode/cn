---
title: 代码质量
slug: /code-quality/
last_update:
  date: 2018-08-18T12:40:00-00:00
---

代码质量可以使用 [SonarCloud](https://sonarcloud.io) 进行分析，该工具由 JHipster 自动配置。


## 在 JHipster 中使用 Sonar

JHipster 提供了特定的 Sonar Docker Compose 配置（[这里是 JHipster Docker Compose 文档](/docker-compose)），提供了开箱即用的 Sonar 实例。在你的项目根目录，请运行：

```shell
docker-compose -f src/main/docker/sonar.yml up -d
```

如果你使用 Maven，它已经被自动配置：

```shell
./mvnw -Pprod clean verify sonar:sonar -Dsonar.login=admin -Dsonar.password=admin
```

如果你需要重新运行 Sonar 阶段，请确保至少指定 `initialize` 阶段，因为 Sonar 属性从 sonar-project.properties 文件加载。

```shell
./mvnw initialize sonar:sonar -Dsonar.login=admin -Dsonar.password=admin
```

如果你使用 Gradle，它也已经被自动配置：

```shell
./gradlew -Pprod clean check jacocoTestReport sonarqube -Dsonar.login=admin -Dsonar.password=admin
```

你也可以使用 [sonar-scanner](https://docs.sonarqube.org/display/SCAN/Analyzing+with+SonarQube+Scanner) 运行分析，如果你已经安装了它。

```shell
sonar-scanner
```

一旦分析完成，它将在 Sonar 仪表板上可用，默认情况下在 [http://127.0.0.1:9001/](http://127.0.0.1:9001/) 上可用。


## 从 Jacoco 分析中排除文件

如果你希望从覆盖率分析中排除某些类（例如生成的类或应用程序类），并且希望在默认的 jacoco html 报告中获得正确的覆盖率，你需要从分析和报告中排除这些类。

### Maven

你需要在单元测试和集成测试的 `prepare-agent` 和 `report` 目标中添加排除：

```xml
<plugin>
    <groupId>org.jacoco</groupId>
    <artifactId>jacoco-maven-plugin</artifactId>
    <version>${jacoco-maven-plugin.version}</version>
    <executions>
        <execution>
            <id>pre-unit-tests</id>
            <goals>
                <goal>prepare-agent</goal>
            </goals>
            <configuration>
                <!-- Exclude any class named Application from instrumentation -->
                <excludes>**/Application.*</excludes>
                <destFile>${jacoco.utReportFile}</destFile>
            </configuration>
        </execution>
        <execution>
            <id>post-unit-test</id>
            <phase>test</phase>
            <goals>
                <goal>report</goal>
            </goals>
            <configuration>
                <!-- Exclude any class named Application from reporting-->
                <excludes>**/Application.*</excludes>
                <dataFile>${jacoco.utReportFile}</dataFile>
                <outputDirectory>${jacoco.reportFolder}</outputDirectory>
            </configuration>
        </execution>
        <execution>
            <id>pre-integration-tests</id>
            <goals>
                <goal>prepare-agent-integration</goal>
            </goals>
            <configuration>
                <!-- Exclude any class named Application from instrumentation -->
                <excludes>**/Application.*</excludes>
                <destFile>${jacoco.itReportFile}</destFile>
            </configuration>
        </execution>
        <execution>
            <id>post-integration-tests</id>
            <phase>post-integration-test</phase>
            <goals>
                <goal>report-integration</goal>
            </goals>
            <configuration>
                <!-- Exclude any class named Application from reporting-->
                <excludes>**/Application.*</excludes>
                <dataFile>${jacoco.itReportFile}</dataFile>
                <outputDirectory>${jacoco.reportFolder}</outputDirectory>
            </configuration>
        </execution>
    </executions>
</plugin>
```

### Gradle

你可以在 `sonar.gradle` 文件中添加以下内容：

```gradle
test {
    jacoco {
        excludes += ['build/generated/**']
    }
}

jacocoTestReport {
    afterEvaluate {
        classDirectories = files(classDirectories.files.collect {
            fileTree(dir: it, exclude: [
                    '**/*_.class'
            ])
        })
    }
}
```


## 默认生成项目的自动分析

JHipster 生成器项目发布了一个示例项目，每次在 "main" 分支中合并新提交时都会对其进行分析：

[在 JHipster 组织下的 JHipster 示例应用分析](https://sonarcloud.io/dashboard?id=jhipster-sample-application)

这使 JHipster 团队能够确保你将在尽可能干净的代码上开始开发你的项目。

这个分析由 [SonarCloud](https://sonarcloud.io) 免费提供。
