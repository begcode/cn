---
title: 部署到 Clever Cloud
slug: /clever-cloud/
last_update:
  date: 2020-10-09T00:00:00-00:00
---
import useBaseUrl from '@docusaurus/useBaseUrl';

[Clever cloud](https://www.clever-cloud.com/) 是一个 IT 自动化平台

[<img src={useBaseUrl('/images/logo/logo_clever_cloud.png')} alt="Clever cloud logo" width="300px" />](https://www.clever-cloud.com/)


## 开始之前

您必须安装 [Clever cloud CLI](https://www.clever-cloud.com/doc/clever-tools/getting_started/)。

您还必须[创建 Clever Cloud 账户](https://api.clever-cloud.com/v2/sessions/signup)并通过运行以下命令 `clever login` 使用 CLI 登录

```shell
Opening https://console.clever-cloud.com/cli-oauth?cli_version=2.7.1&cli_token=XXX in your browser to log you in…
Login successful as ...
```


## 创建您的 Clever Cloud 应用程序

1. 如果您使用 maven `clever create --type maven [您的应用程序名称]` 或使用 gradle `clever create --type gradle [您的应用程序名称]`

2. 向您的应用程序添加数据库附加组件 `clever addon create [附加组件提供商] [您的附加组件名称] --link [您的应用程序名称]`

    列出支持的附加组件提供商 `clever addon providers`
    ```
    cellar-addon      Cellar S3 storage       S3-like online file storage web service
    config-provider   Configuration provider  Expose configuration to your applications  (via environment variables)
    es-addon          Elastic Stack           Elasticsearch with Kibana and APM server as options
    fs-bucket         FS Buckets              Persistent file system for your application
    mongodb-addon     MongoDB                 A noSQL document-oriented database
    mysql-addon       MySQL                   An open source relational database management system
    postgresql-addon  PostgreSQL              A powerful, open source object-relational database system
    redis-addon       Redis                   Redis by Clever Cloud is an in-memory key-value data store, powered by Clever Cloud
    ```

    [查看支持的附加组件](https://www.clever-cloud.com/doc/addons/clever-cloud-addons/#available-add-ons)

3. 设置环境变量 `clever env set CC_PRE_RUN_HOOK "cp ./clevercloud/application-clevercloud.yml ./application-prod.yml"`

4. 启用专用构建 `clever scale --build-flavor M`

    [查看专用构建](https://www.clever-cloud.com/doc/admin-console/apps-management/#dedicated-build)


## 配置您的 JHipster 应用程序

1. 在您的项目中添加一个 `clevercloud/` 文件夹。

2. 创建 `clevercloud/application-clevercloud.yml` 以使用预定义的 clever cloud 附加组件环境变量

    对于 PostgreSQL

    ```yaml
    spring:
      datasource:
        type: com.zaxxer.hikari.HikariDataSource        
        url: jdbc:postgresql://${POSTGRESQL_ADDON_HOST}:${POSTGRESQL_ADDON_PORT}/${POSTGRESQL_ADDON_DB}?useUnicode=true&characterEncoding=utf8&useSSL=false
        username: ${POSTGRESQL_ADDON_USER}
        password: ${POSTGRESQL_ADDON_PASSWORD}
        hikari:
          maximumPoolSize: 2
    ```

    对于 MySQL

    ```yaml
    spring:
      datasource:
        type: com.zaxxer.hikari.HikariDataSource        
        url: jdbc:mysql://${MYSQL_ADDON_HOST}:${MYSQL_ADDON_PORT}/${MYSQL_ADDON_DB}?useUnicode=true&characterEncoding=utf8&useSSL=false
        username: ${MYSQL_ADDON_USER}
        password: ${MYSQL_ADDON_PASSWORD}
        hikari:
          maximumPoolSize: 2
    ```

    对于 MongoDB

    ```yaml
    spring:
      data:
        mongodb:
          uri: ${MONGODB_ADDON_URI}
          database: ${MONGODB_ADDON_DB}
    ```



3. 添加一个包含 goal 字段的 json 文件，以指示如何启动您的应用程序

  对于 maven
  创建 `clevercloud/maven.json` 文件并使用您的 pom.xml **artifactId**
  
  ```json
  {
    "build": {
      "type": "maven",
      "goal": "-Pprod package -DskipTests"
    },
    "deploy": {
    "jarName": "./target/[替换为 ARTIFACTID]-0.0.1-SNAPSHOT.jar"
    }
  }
  ```

  对于 gradle
  创建 `clevercloud/gradle.json` 文件并使用 gradle.properties **rootProject.name**

  ```json
  {
    "build": {
      "type": "gradle",
      "goal": "-Pprod bootJar -x test"
    },
    "deploy": {
      "jarName": "./build/libs/[替换为 rootProject.name]-0.0.1-SNAPSHOT.jar"
    }
  }
  ```


## 部署您的应用程序

### 使用 CLI

您必须在部署前提交

```shell
git commit -m "Clever deploy"
```

然后运行：

```shell
clever deploy
```

### 使用 gitlab CI

在 gitlab CI/CD 环境变量中定义 `$CLEVER_TOKEN` 和 `CLEVER_SECRET`

将此阶段添加到您的 `.gitlab-ci.yml`

```yaml
deploy-to-clever-env:
  stage: deploy
  variables:
    APP_NAME: [clever cloud 应用程序名称]
    APP_ID: [clever cloud 应用程序 ID]
  script:
    - wget https://clever-tools.cellar.services.clever-cloud.com/releases/latest/clever-tools-latest_linux.tar.gz
    - tar xvzf clever-tools-latest_linux.tar.gz
    - ./clever-tools-latest_linux/clever login --token $CLEVER_TOKEN --secret $CLEVER_SECRET
    - ./clever-tools-latest_linux/clever link ${APP_ID}
    - ./clever-tools-latest_linux/clever deploy -a ${APP_NAME}
  environment:
    name: [环境名称]
    url: https://${APP_NAME}.cleverapps.io
```


## 使用 Github Action

在 Github secret 中定义 `CLEVER_TOKEN` 和 `CLEVER_SECRET` (Settings > Secret)

将此步骤添加到您的 `.github-action.yml`

```yaml
- uses: actions/checkout@v2
- name: 部署到 cc
  env:
    APP_NAME:[clever cloud 应用程序名称]
    APP_ID: [clever cloud 应用程序 ID]
  run: |
    git fetch --prune --unshallow
    wget https://clever-tools.cellar.services.clever-cloud.com/releases/latest/clever-tools-latest_linux.tar.gz
    tar xvzf clever-tools-latest_linux.tar.gz
    ./clever-tools-latest_linux/clever login --token ${{ secrets.CLEVER_TOKEN }} --secret ${{ secrets.CLEVER_SECRET }}
    ./clever-tools-latest_linux/clever link ${{ env.APP_ID }}
    ./clever-tools-latest_linux/clever deploy -f -a ${{ env.APP_NAME }}
```


## 更改 Java 版本

您可以选择 Java 版本（默认为 Java 11）

```shell
clever env set CC_JAVA_VERSION 12
```


## 更多信息

* [Clever Cloud 文档](https://www.clever-cloud.com/doc/)
* [Clever Cloud Java maven 部署](https://www.clever-cloud.com/doc/java/java-maven/)
* [Clever Cloud Java gradle 部署](https://www.clever-cloud.com/doc/java/java-gradle/)