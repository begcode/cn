---
title: 如何创建新的权限
slug: /tips/025_tip_create_new_authority.html
last_update:
  date: 2018-10-05T18:20:00-00:00
---

__Tip submitted by [@Tonterias](https://github.com/Tonterias)__

假设你需要除了给定的 ADMIN 和 USER 之外的新权限。让新权限被称为 `ROLE_EXAMPLE_AUTHORITY`。

修改 AuthoritiesConstants.java 文件以包含你的新权限：

```java
/**
 * Constants for Spring Security authorities.
 */
public final class AuthoritiesConstants {

	public static final String ADMIN = "ROLE_ADMIN";

	public static final String USER = "ROLE_USER";

	public static final String ANONYMOUS = "ROLE_ANONYMOUS";
	
	public static final String EXAMPLE_AUTHORITY = "ROLE_EXAMPLE_AUTHORITY";

	private AuthoritiesConstants() {
	}
}
```

不要忘记在你的 `authority.csv` 中包含你的新角色：

```csv
name
ROLE_ADMIN
ROLE_USER
ROLE_ANONYMOUS
ROLE_EXAMPLE_AUTHORITY
```

这样，你就可以在 SecurityConfiguration.java 中使用它：

```java
@Override
public void configure(HttpSecurity http) throws Exception {
	// @formatter:off
	http
		.csrf()
		.disable()
		...
	...
	.and()
		.authorizeRequests()
		...
		.antMatchers("/newresource/**").hasAuthority(AuthoritiesConstants.ROLE_EXAMPLE_AUTHORITY)
...
}
```

在你的控制器层（例如 `FrontPageConfigResource.java`）中：

```java
@DeleteMapping("/order-items/{id}")
@Timed
@PreAuthorize("hasAuthority('ROLE_EXAMPLE_AUTHORITY')")
public ResponseEntity<Void> deleteOrderItem(@PathVariable Long id) {
	...
}
```

在你的 Angular html 文件中：`jhiHasAnyAuthority=['ROLE_ADMIN', 'ROLE_EXAMPLE_AUTHORITY' ...]`

在你的 Angular 路由中：

```ts
export const messageRoute: Routes = [
	{
		path: 'message',
		component: MessageComponent,
		data: {
			authorities: ['ROLE_EXAMPLE_AUTHORITY'],
			pageTitle: 'Messages'
		},
		canActivate: [UserRouteAccessService]
	}
];
```

开源示例在 JhipsterPress：https://github.com/Tonterias/JhipsterPress
